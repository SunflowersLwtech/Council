[pytest]
asyncio_mode = auto
pythonpath = .
markers =
    live_api: tests that require live API keys (Mistral, ElevenLabs, Gemini)
    voice: tests that require elevenlabs SDK or voice infrastructure
addopts = -m "not live_api and not voice"
testpaths = tests
